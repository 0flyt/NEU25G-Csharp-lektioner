<Window x:Class="L043_Panelss.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:L043_Panelss"
        xmlns:controls="clr-namespace:L043_Panelss.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="stackPanel" >
            <Button
                Content="1"
                HorizontalAlignment="Left"
                Width="20"
                Margin="10 10 0 0"
                Click="StackPanelButton_Click"
            />
        </StackPanel>

        <WrapPanel x:Name="wrapPanel" Orientation="Vertical" Grid.Column="1">
            <Button
                Content="1"
                HorizontalAlignment="Left"
                Width="20"
                Margin="10 10 0 0"
                Click="WrapPanelButton_Click"
            />  
        </WrapPanel>
        <DockPanel x:Name="dockPanel" Grid.Column="0" Grid.Row="1">
            <Button Content="L" DockPanel.Dock="Left" Click="DockPanelButton_Click"/>
           
        </DockPanel>

        <controls:ExampleUserControl Grid.Column="1" Grid.Row="1" />
    </Grid>
</Window>
