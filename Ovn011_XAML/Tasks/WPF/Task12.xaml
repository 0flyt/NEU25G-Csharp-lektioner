<Page x:Class="Ovn011_XAML.Tasks.WPF.Task12"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ovn011_XAML.Tasks.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Task12"
      >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Canvas 
            Grid.Column="1"
            Grid.Row="1"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
        >
            <Label
                x:Name="positionText"
                Content="text"
            />
            
        </Canvas>

        <StackPanel
            Grid.Column="1"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Center"
            MinWidth="100"
        >
            <Label Content="X-Position:" HorizontalAlignment="Center" />
            <Slider
                x:Name="xSlider"
                ValueChanged="xSlider_ValueChanged"
                IsSnapToTickEnabled="True"
                TickFrequency="1"
                Minimum="-50"
                Maximum="50"
                
            />
        </StackPanel>
        
        <StackPanel
            Grid.Column="0"
            Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            MaxHeight="100"
        >
            <Label Content="Y-Position:" VerticalAlignment="Center" />
            <Slider 
                x:Name="ySlider"
                ValueChanged="ySlider_ValueChanged"
                IsSnapToTickEnabled="True"
                TickFrequency="1"
                Minimum="-50"
                Maximum="50"
                Orientation="Vertical"
                
            />
        </StackPanel>
    </Grid>
</Page>
