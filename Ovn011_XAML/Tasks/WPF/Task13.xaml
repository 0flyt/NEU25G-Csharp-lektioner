<Page x:Class="Ovn011_XAML.Tasks.WPF.Task13"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ovn011_XAML.Tasks.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Task13">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Orientation="Vertical">
                <Slider
                    x:Name="positionSlider"
                    ValueChanged="positionSlider_ValueChanged"
                    Width="100"
                    TickFrequency="1"
                    Ticks="100"
                    AutoToolTipPlacement="TopLeft"
                    Margin="0 20"
                    Maximum="100"
                    Minimum="0"
                />
            </StackPanel>
            <CheckBox Content="Lock X-value" x:Name="checkBoxX" Click="checkBoxX_Click" Margin="0 10" />
            <CheckBox Content="Lock Y-value" x:Name="checkBoxY" Click="checkBoxY_Click" />
            
        </StackPanel>
        <Canvas Grid.Row="1" Width="50" Height="100" >
            <Label x:Name="labelText" Content="X: 0, Y: 0" />
        </Canvas>
    </Grid>
</Page>
