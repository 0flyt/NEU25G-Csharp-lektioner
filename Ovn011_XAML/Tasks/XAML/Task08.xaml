<Page x:Class="Ovn011_XAML.Task08"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ovn011_XAML"
        mc:Ignorable="d"
        Title="Task08" d:Height="450" d:Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Label Content="Students:" Grid.Row="0" VerticalAlignment="Bottom" />

        <ListBox Grid.Row="1" x:Name="MyList" SelectionChanged="MyList_SelectionChanged" />

        <StackPanel 
            Grid.Row="1"
            Grid.Column="1"
            VerticalAlignment="Top"
            Margin="40 0 40 0"
            
            >
            <Label Content="Firstname:" />
            <TextBox x:Name="firstname" Margin="0 0 0 20" />

            <Label Content="Lastname:" />
            <TextBox x:Name="lastname" Margin="0 0 0 20" />

            <Label Content="Email:" />
            <TextBox x:Name="email" Margin="0 0 0 20" />

            <Button Content="OK" Width="40" />
        </StackPanel>
    </Grid>
</Page>
